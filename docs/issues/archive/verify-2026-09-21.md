# 根因核对（BUG-0003 / 0004 / 0006）

状态：完成，2026-09-21。对 `fab7137` 独立复核，结论与 [reliability](reliability.md)、[pr-2-review](pr-2-review.md) 一致。

## 已修复

- **BUG-0003**（reduce 静默放过未知事件）
  - 7 个既有场景与新增 `appointments`、`continuous-crisis` 的 `resolveBatch` 与 `reduce` 默认分支均改为抛错。
  - 两个具体缺口已补投影分支：`contradictory-orders.ts:686` 的 `operation.failed`、`dynamic-promotion.ts:596` 的 `artifact.disposition_recorded`。
  - `reliability.test.ts:61` 断言全部投影拒绝未知事件，`:147` 断言不可达行动保留失败状态而不移动单位。
- **BUG-0004**（payload 静默降级）
  - 新增 `packages/shared-types/src/read-json.ts` 的 `readJsonObject` / `readStringArray`（zod，畸形即抛错）。
  - `reliability.test.ts:83` 显式区分"损坏值"与"真空集合"；决策评分与因素数组损坏也显式失败。
- **BUG-0006**（网页吞错误）
  - `apps/web/src/run-failure.tsx` 保留错误原因并提供重开入口；`live-plugin.ts:66`、`live-service.ts:207` 均写回 `error.message`，初始化失败不再被加载态遮蔽。

## 残留现状

沿用 PR #2 review 的"暂不处理"结论，此处仅登记事实，不重新审批：

- **BUG-0005**：`packages/agent-runtime/src/deepseek-harness.ts:101-112` 仍做去围栏 + 首尾花括号截取的宽松解析。
- **BUG-0001**：`packages/localization/src/index.ts:26` 仍 `?? en[key]`。
- **BUG-0002**：`packages/localization/src/index.ts:36` 仍 `replaceAll("_", " ")` 造词。
- **类型层缺口**：`DomainEvent.eventType` 仍为开放 `string`（FEAT-0002/0003 并入可靠性修复后未做全项目事件协议重构）。新场景漏投影现在会**显式失败**，但编译期仍无穷尽性保证。
